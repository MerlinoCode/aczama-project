import './Footer.scss';

const Footer: React.FC = () => {
    return (
        <footer className="footer">
            Place some footer content here.
        </footer>
    );
}

export default Footer;